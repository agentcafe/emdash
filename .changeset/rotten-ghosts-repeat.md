---
"@emdash-cms/plugin-ats": patch
---

Adds admin UI pages (jobs dashboard, create job, edit job, candidates list, candidates profile) with Kumo components, API integration via emdash/plugin-utils, and AI-assisted job description generation. Fixes CreationAgent type declarations.
