---
description: Create a pull request from the current branch against emdash-cms/emdash
agent: github
---

Create a PR from the current branch. The GitHub agent will verify quality gates (lint, typecheck, tests, changeset), fill the PR template, and create the PR against `emdash-cms/emdash:main` from `agentcafe/emdash`.
