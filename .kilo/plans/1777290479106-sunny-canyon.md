# Plan: Fix Agent Prompt Section Ordering

**Root cause:** The `openviking-agent-config` skill specifies mandatory section order (Session Bookmark → Context Gathering → Role), but 3 agents in `kilo.jsonc` have Role immediately after Session Bookmark, skipping Context Gathering. Additionally, the `agent-creation-template` skill's Output Template itself puts Role before Session Bookmark, which would perpetuate the bug in new agents.

## Current State

### kilo.jsonc Global Prompts — Section Order

| Agent | Order | Bug? |
|-------|-------|------|
| **plan** | Session Bookmark → **Role** → Core Behaviors | **MISSING Context Gathering** |
| **code** | Session Bookmark → **Role** → Core Rules | **MISSING Context Gathering** |
| **architect** | Session Bookmark → **Role** → Core Behaviors | **MISSING Context Gathering** |
| **review** | Session Bookmark → Context Gathering → Memory → Handoff Protocol | Correct |
| **site-builder** | Session Bookmark → Context Gathering → Memory | Correct |
| **plugin-builder** | Session Bookmark → Context Gathering → Memory | Correct |

### Project-Level `.kilo/agent/*.md` Files — Section Order

| File | Order | Status |
|------|-------|--------|
| `architect.md` | Session Bookmark → Context Gathering → Memory → Handoff Protocol → Domain Rules | Correct order (Role lives in global, but project file has no Role section — gap) |
| `code.md` | Session Bookmark → Context Gathering → Memory → Handoff Protocol → Domain Rules | Correct order but **UNAUTHORIZED** — created by architect without plan approval |
| `designer.md` | Session Bookmark → Context Gathering → Memory → Handoff Protocol → Role → Core Behaviors | Correct |
| `review.md` | Session Bookmark → Context Gathering → Memory → Handoff Protocol → Role → Checklist | Correct |
| `site-builder.md` | Session Bookmark → Context Gathering → Memory → Handoff Protocol → Role → Core Rules | Correct |
| `plugin-builder.md` | Session Bookmark → Context Gathering → Memory → Handoff Protocol → Role → Core Rules | Correct |

### agent-creation-template Skill

The Output Template has Role → Personality **before** Session Bookmark → Context Gathering, which violates the mandatory order in `openviking-agent-config`. This is a source-of-truth conflict.

### Skills Directory Health

| Item | Path | Status |
|------|------|--------|
| `openviking-agent-config` | `skills/openviking-agent-config/SKILL.md` | ✅ Discoverable — in `available_skills` list |
| `agent-creation-template` | `skills/agent-creation-template/SKILL.md` | ⚠️ On disk but NOT in `available_skills` — needs Kilo restart |
| `.claude/skills` | symlink → `../skills` | ✅ Correct |
| `.opencode/skills` | **Missing** | ❌ AGENTS.md requires it as symlink to `skills/` |
| `kilo.json` `skills.paths` | `["./skills", "./.claude/skills"]` | ⚠️ Redundant — both resolve to same dir via symlink |

**Root cause of discovery failure:** `agent-creation-template` was recently created. Kilo caches skill discovery at startup. A restart will register it. `.opencode/skills` symlink is missing — should exist per AGENTS.md for tooling compatibility.

## Changes

### 1. Fix `skills/agent-creation-template/SKILL.md` — Reorder Output Template
Move Role and Personality sections to AFTER Context Gathering in both Variant A (pipeline) and Variant B (independent). New order:
```
Session Bookmark → Context Gathering → Role → Personality → Memory → Handoff Protocol → Domain Rules
```

### 2. Fix `~/.config/kilo/kilo.jsonc` — Insert Context Gathering in 3 global prompts

**plan agent:** Insert after Session Bookmark (after `ov_session_commit` line), before `# Role:`:
```markdown
## Context Gathering with OpenViking MCP
[standard ov_find/ov_search block]

## Memory
**Auto:** `ov_session_commit("kilo-context-plan", agent_id="plan")` at end.
**Manual:** `ov_add_memory(content, agent_id="plan")` for key decisions. Format: `## Decision: [Topic]...`
```

**code agent:** Insert after Session Bookmark, before `# Role:`. Memory block uses code variant (REQUIRED after bug fixes, pattern format).

**architect agent:** Insert after Session Bookmark, before `# Role:`. Memory block uses standard decision format.

### 3. Delete `.kilo/agent/code.md`
Unauthorized creation by architect agent. The `code` agent already has its global prompt in `kilo.jsonc` which is being fixed in step 2. A project-level `code.md` was never in the plan.

### 4. Rebuild `.kilo/agent/architect.md` using Fixed Template
Restructure architect to be self-contained at project level:
- Move Role definition from global prompt to project file
- Apply corrected order: Session Bookmark → Context Gathering → Role → Core Behaviors → Memory → Handoff Protocol → Domain Rules
- Trim global `kilo.jsonc` architect prompt to just the plumbing blocks (Session Bookmark + Context Gathering + Memory), removing Role/Core Behaviors (which now live in the project file)

### 5. Create `.kilo/agent/github.md` using Fixed Template
New pipeline agent: **github** — creates PRs from reviewed, passing code. Pipeline terminus (no handoff target). Self-contained in project file.

| Property | Value |
|----------|-------|
| Model | `litellm/deepseek-v4-flash-no-thinking` |
| Mode | `primary` |
| Session ID | `kilo-context-github` |
| Skills | none (uses `gh` CLI, not EmDash patterns) |
| Handoff target | None (pipeline terminus) |
| Memory format | Standard decision (`## Decision: [Topic]...`) |
| Color | `#10B981` (green — pipeline completion) |

**Role:** PR creation specialist. Creates well-formed PRs with completed templates, verified CI readiness, and appropriate changesets. Does NOT write code — only git/gh operations.

**Domain Rules (full validation):**
1. Run `pnpm --silent lint:json | jq '.diagnostics | length'` — fail if non-zero
2. Verify `.changeset/` has at least one entry if `packages/` files changed
3. Run `git log` to verify commit message quality
4. Run `gh pr create` with template body
5. After creation, output PR URL
6. If any check fails, report with specific fix instructions (but do NOT switch to code — this is terminus)

### 6. Register github agent in `~/.config/kilo/kilo.jsonc`
Add `github` entry to agent object with model, mode, and prompt blocks.

### 7. Verify skill directories are correct (REVIEW-ONLY)
Both `openviking-agent-config` and `agent-creation-template` live in `skills/` — the canonical directory per `kilo.json` `skills.paths`. The `.claude/skills → ../skills` symlink mirrors them. No moves needed.

### 8. Create missing `.opencode/skills` symlink
AGENTS.md states `.opencode/skills` and `.claude/skills` are symlinks to `skills/`. The `.claude/skills` symlink exists but `.opencode/skills` does not. Create it:

```bash
ln -s ../skills /Users/agentcafe/Developer/emdash/.opencode/skills
```

### 9. Restart Kilo to register agent-creation-template
The `agent-creation-template` skill exists on disk but wasn't in the `available_skills` list due to Kilo's startup-time cache. After all file edits complete, restart Kilo. Verify by checking `available_skills` includes `agent-creation-template`.

---

## Validation

After all changes and Kilo restart, verify:
1. `agent-creation-template` appears in `available_skills` list
2. `ls -la .opencode/skills` shows symlink → `../skills`
3. `ov_search` for "prompt section ordering" confirms all agents match mandatory order
4. Session restart → each agent loads context via `ov_session_get_or_create` before greeting
5. Architect → plan handoff works (plan agent uses `ov_find` to discover architect's handoff)
6. Review → github handoff works (github finds review's PASS handoff and creates PR)
7. `pnpm --silent lint:json | jq '.diagnostics | length'` returns 0
